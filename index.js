//This is only a Testing File with HTML. I am not fully learned with JavaScript but if you do a seprated file with this, It would be easier for JavaScript. It's Basically Discord.js
const discord = require("discord.js");
const config = require('./config.json')
const keepAlive = require('./server.js');















keepAlive();
